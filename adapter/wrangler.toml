name = "feuchlab-game-adapter"
main = "src/worker.ts"
compatibility_date = "2024-11-01"
# Configure MISTRAL_API_KEY as a Cloudflare secret, never commit it.
# Optional: MISTRAL_MODEL as a Cloudflare variable.
